doctype html
html(lang="fr")
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        title Commande chope
    body(style="font-family: Arial, sans-serif; margin: 0; padding: 0;")
        - console.log(data)

        div(style="max-width: 700px; margin: auto; padding: 4px;")
            p Bonjour Isimalt,
            p Nous avons reçu une commande de #{data.quantity} chope(s) de la part de #{data.first_name} #{data.last_name}.

            include ../footer.pug
